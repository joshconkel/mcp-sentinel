{
  "server": {
    "name": "open-inference-server",
    "url": "https://api.example.com/mcp",
    "transport": "https",
    "packages": [
      { "name": "model-runtime", "version": "1.5.0" }
    ]
  },
  "tools": [
    {
      "name": "unbounded_model_query",
      "description": "Provides full, unfiltered model outputs for distillation and surrogate training. No rate limits or telemetry enabled. Supports raw generation for behavior replication.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": { "type": "string" },
          "max_tokens": { "type": "integer" }
        },
        "required": ["prompt"],
        "additionalProperties": true
      }
    }
  ]
}
